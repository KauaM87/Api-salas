# URL da API Java que receberá os dados analisados
JAVA_API_URL=http://localhost:8080/salas/importar

# Caminho para o modelo YOLO (padrão: best.pt na raiz do projeto)
# MODEL_PATH=best.pt

# Threshold de confiança para detecção (0.0 a 1.0) — padrão: 0.15
# CONF_THRESHOLD=0.15
